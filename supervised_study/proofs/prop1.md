# Proposition 1 -- Conditioning under Private Affine Energies

Verification of `theory.tex`, Proposition `prop:private-affine`. Generated by `proofs/prop1.py`.

- **Gradient block = r_j * xt**, `||grad|| <= ||xt||`: analytic vs finite-difference max error `1.50e-09`; worst norm/bound ratio `0.965` (<= 1). **PASS**
- **Hessian = -C(r) (x) xt xt^T (Kronecker)**, `||H|| <= (1/2)||xt||^2`: form matches numeric Hessian to `4.31e-08`; worst norm/bound ratio `0.708` (<= 1). **PASS**

The Kronecker-product spectral-norm step `||A (x) B||_2 = ||A||_2 ||B||_2` is what turns Lemma 1's `1/2` and `||xt||^2` into the parameter-independent constant `(1/2) E||xt||^2` under batch-mean training.

**Overall: ALL CHECKS PASS.**

_Scope: confirms the analytic gradient/Hessian forms and the resulting bounds at sampled points; not a general-parameter theorem prover._
