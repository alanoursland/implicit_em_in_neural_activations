# Lemma 1 -- Simplex Invariant and Curvature Bound

Verification of `theory.tex`, Lemma `lem:simplex`. Generated by `proofs/lemma1.py`.

- **grad_d L_LSE = r**: analytic vs finite-difference max error `2.41e-10`; sums to 1, nonnegative. **PASS**
- **Hessian = rr^T - diag(r)**, C(r) PSD: max error `1.81e-08`. **PASS**
- **||C(r)||_2 <= 1/2**: max over 20k samples per K in {2,3,10,25} is `0.50000`; two-point value `0.50000` (tight). **PASS**
- **Popoviciu step (symbolic)**: v^T C(r) v = Var_(j~r)(v_j) confirmed identically. **PASS**

**Overall: ALL CHECKS PASS.**

_Scope: confirms the claimed identities/constants at sampled and witnessed points and via the symbolic variance identity; not a general-d theorem prover._
