# Proposition 1' -- Smooth Monotone Kernels

Verification of `theory.tex`, Proposition `prop:smooth-kernel`. Generated by `proofs/prop1_smooth.py`.

Role: reassurance that parameter-independence survives the smooth kernel the experiments use. The paper spends the *qualitative* claim, not the exact constant.

- **Softplus (B1=1, B2=1/4)**: `||Hess_theta L||_2 <= 0.75||xt||^2`; worst numeric/bound ratio `0.067` (bound valid, and loose). **PASS**
- **ReLU (a.e. + absorbing state)**: away from kinks the bound reduces to Prop 1's `0.5||xt||^2` (B1=1, B2=0), worst numeric/bound ratio `0.014`; a component inactive on the whole batch has finite-difference gradient-block norm `0.00e+00` (the documented absorbing-state pathology, not a bound violation). **PASS**

**Overall: ALL CHECKS PASS.**

_Scope: confirms the parameter-independent bound holds for the kernels used experimentally. The constant is not tight and is not used downstream; the qualitative claim is what the paper relies on._
