This section is doing exactly what I hoped a consolidated limitations section would do — and I'll note something genuinely impressive about it: nearly every threat listed here is the *correct* threat, stated at the correct strength, including several I raised across this conversation (matched-optimizer, cosine calibration, probe-dependence, basin caution, guarantee-forfeiture scope). A reviewer reading this section will conclude the authors found the weaknesses before they did, which is the entire point. Remaining notes:

**What's still missing from the list**

1. **The E‖x̃‖² promise — still unresolved, and this section was its last natural home.** The theory section commits to reporting the constant and comparing 2/L against the stop-gradient learning-rate tolerance. No results section delivers it, and it's absent here too. This is now the only thread from the theory that dangles with no acknowledgment anywhere. Three resolutions, any of which works: (a) run the comparison and report it in Results III; (b) add a paragraph here — "the descent-lemma comparison promised in Section 4 is [status]"; (c) delete the promise from the theory section. What can't survive a careful reviewer is the current state: an explicit quantitative prediction made and silently dropped.

2. **Metric–objective coupling from Results I.** The dead-unit and redundancy metrics are near-relatives of the variance and decorrelation training terms — the arms are partially evaluated on quantities they were optimized for. Results I's own cross-evidence (Var-only *worsens* redundancy 9×) shows the metrics aren't trivially co-optimized, which is the defense; but the coupling deserves a named paragraph here, because it's the most obvious objection to Experiment 1 and it's currently unacknowledged anywhere.

3. **The one-sided sensitivity sweep.** Both Results II and III show learning-rate sensitivity entirely as low-η slowness — no arm is swept upward to instability, so the "spread" metrics conflate convergence speed with the smoothness story the theory tells. This is an internal-validity threat of the same rank as the matched-optimizer one, and it belongs in the list (or better, gets fixed with one extra η rung).

4. **The auxiliary objective is unbounded below.** The variance runaway at high-η Adam (min var 30628) revealed a flat/escape direction, plausibly manufactured by LayerNorm's scale invariance. This is a limitation of the objective itself and it interacts with the single-layer conditioning story (the "fixed smoothness constant" claim is about L_LSE, not the full trained objective). One paragraph: the auxiliary objective has degrees of freedom unconstrained by the task; the operating point was chosen below the runaway regime; the clean curvature claims apply to the LSE term. This closes the last theory-side gap I've been tracking.

5. **Adam-under-stop-gradient.** The optimizer-signature claim is tested in one direction only (Adam helps in the composed model) — the prediction that Adam *stops* helping under stop-gradient is stated but untested. It's in the future-work blocks but it's genuinely a validity threat to the signature claim, so it belongs in this list too, or — better, since it's one flag flip — in the results.

**Structural**

This section now overlaps heavily with the two "Future Strengthening Analyses" blocks in Setup and Results III — the denser λ curve, the basin analysis, and the matched-SGD measurement each appear in two or three places. Consolidate: threats live here, the future-work framing gets one short paragraph (here or in the conclusion), and both upstream blocks get deleted. Repetition of the same caveat three times reads as anxiety rather than rigor; once, in the right place, reads as command of the material.

**Fine points on what's here**

- The stop-gradient paragraph's closing sentence — "it shows that the CE corridor is sufficient to destroy the conditioning signature when it dominates" — is precisely calibrated. Keep verbatim.
- The linear-probe paragraph proposes kNN/MLP probes as the check. Note these are cheap (frozen features already saved, presumably) — same triage question as elsewhere: things this inexpensive that you've named as the test are usually better run than listed.
- "The formal results are sufficient conditions, not an if-and-only-if" — good, and it quietly honors the scope paragraph in the theory. One addition: the *destroying* direction is also not proven necessary (a dense corridor forfeits the *guarantee*; you correctly say some composed systems may stay well-conditioned anyway). You do say this — just confirming it survived, because it's the sentence that keeps Prop 2 honest.
- Cosine paragraph: consistent with Results III now. If you take my earlier note that ±0.01 *undershoots* the 0.2 null scale, this paragraph is where the one-line acknowledgment would go ("the observed values are in fact smaller than the random-direction scale; we do not interpret this further").

**Style** — Register is right throughout; the paragraph-headline format is the correct one for this section. Only nit: "The paper should not claim state-of-the-art performance" — phrase as "we do not claim..." rather than instructions to the paper; there are two or three spots in this section where draft-notes-to-self voice ("the final presentation should report...") leaks through. Convert those to either commitments ("we report...") or limitations ("we did not vary...").

Net: this section, once it absorbs items 1–4 and the deduplication, will be the strongest limitations section I've seen in this genre of paper — it's already close. The full-paper consistency pass is still on offer once the conclusion (if there is one) arrives; at this point the ledger of open items across the whole draft is: E‖x̃‖² (decide), matched-SGD ratio (run), Adam stop-gradient arm (run), upper-η rung (run), K=64 λ-recalibration (run or soften), abstract fixes (affine qualifier, "confirm"→"support," title self-reference), ½-saturation early definition, NLS naming, and the future-work consolidation. Nothing on that list is structural.